import { createServer } from "node:http";
import { readFileSync } from "node:fs";
import { fileURLToPath } from "node:url";
import { config } from "./config.mjs";
import { Store } from "./db.mjs";
import { SupabasePersistence } from "./supabase.mjs";
import { Trader } from "./trader.mjs";
import { authorized, body, send, log, safeMessage } from "./http.mjs";
import { hash } from "./intelligence.mjs";
export function createApp(c,store,trader){return createServer(async(req,res)=>{try{const u=new URL(req.url,"http://localhost");if(req.method==="GET"&&u.pathname==="/healthz")return send(res,200,{alive:true,service:"astratrader",mode:c.mode,persistence:c.persistence});if(req.method==="GET"&&u.pathname==="/"){res.writeHead(200,{"content-type":"text/html; charset=utf-8","content-security-policy":"default-src 'self'; script-src 'unsafe-inline'; style-src 'unsafe-inline'; connect-src 'self'; frame-ancestors 'none'; base-uri 'none'","x-content-type-options":"nosniff","cache-control":"no-store"});return res.end(readFileSync(new URL("../public/index.html",import.meta.url)))}if(req.method==="POST"&&u.pathname==="/webhooks/solana"){if(!authorized(req,c.webhookToken))return send(res,401,{error:"Unauthorized webhook"});const items=await body(req);if(!Array.isArray(items)||items.length>1000)return send(res,400,{error:"Expected up to 1000 transactions"});let added=0;store.tx(()=>{for(const item of items){if(!/^[1-9A-HJ-NP-Za-km-z]{80,90}$/.test(item.signature??""))throw Error("Invalid signature");if(store.enqueue(item.signature,{signature:item.signature,source:"webhook"}))added++}});await store.flush();return send(res,202,{queued:added})}if(u.pathname.startsWith("/internal/signer/")){if(!authorized(req,c.signerToken))return send(res,401,{error:"Unauthorized signer"});if(req.method==="GET"&&u.pathname==="/internal/signer/commands")return send(res,200,{kill:store.get("signerKillRequested",false)||store.get("killed",false),orders:store.orders().filter(o=>o.mode==="live"&&["created","queued"].includes(o.state)).slice(0,5).map(o=>o.intent)});if(req.method==="POST"&&u.pathname==="/internal/signer/report"){const report=await body(req,262144);if(!Array.isArray(report.checks)||!Array.isArray(report.orders)||report.orders.length>500||typeof report.holdings!=="object")return send(res,400,{error:"Invalid signer report"});store.tx(()=>{store.entity("signer","current",{...report,at:Date.now()});for(const r of report.orders){const old=store.order(r.id);if(old?.mode==="live"&&["signed","submitted","unknown","confirmed","failed","rejected"].includes(r.state)){store.saveOrder({...old,...r});if(old.state!==r.state)store.event("SIGNER_ORDER_STATE",{id:r.id,state:r.state,signature:r.signature})}}});await store.flush();return send(res,200,{ok:true})}return send(res,404,{error:"Not found"})}if(!authorized(req,c.apiToken))return send(res,401,{error:"Unauthorized"});if(req.method==="GET"&&u.pathname==="/api/state")return send(res,200,trader.state());if(req.method==="GET"&&u.pathname==="/api/events"){const after=Number(u.searchParams.get("after")??0);if(!Number.isSafeInteger(after)||after<0)return send(res,400,{error:"Invalid cursor"});const events=store.events(after,500);return send(res,200,{events,next:events.at(-1)?.seq??after,hasMore:events.length===500})}if(req.method==="GET"&&u.pathname==="/api/export"){const after=Number(u.searchParams.get("after")??0);if(!Number.isSafeInteger(after)||after<0)return send(res,400,{error:"Invalid cursor"});const events=store.events(after,500);return send(res,200,{events,next:events.at(-1)?.seq??after,hasMore:events.length===500,datasetHash:hash(events),mode:c.mode})}if(req.method==="GET"&&u.pathname==="/api/ready")return send(res,200,{mode:c.mode,ready:trader.state().liveChecks.every(x=>x[1])&&trader.state().signer?.checks.every(x=>x[1])===true,checks:trader.state().liveChecks,signer:trader.state().signer??null});if(req.method==="GET"&&u.pathname==="/metrics"){res.writeHead(200,{"content-type":"text/plain; version=0.0.4"});return res.end(`astratrader_ingestion_lag_seconds ${Math.max(0,trader.state().health.lagMs/1000)}\nastratrader_transactions_total ${store.get("transactions",0)}\nastratrader_killed ${store.get("killed",false)?1:0}\n`)}if(req.method==="POST"&&u.pathname==="/api/control"){const b=await body(req,2048);if(!["pause","resume","kill"].includes(b.action))return send(res,400,{error:"Unknown action"});store.tx(()=>{if(b.action==="kill")store.set("killed",true);else store.set("paused",b.action==="pause");store.event("OPERATOR_CONTROL",{action:b.action})});await store.flush();if(b.action==="kill"&&c.mode==="live"){try{await trader.signer("/kill","POST",{})}catch{store.event("SIGNER_KILL_UNCONFIRMED",{});return send(res,503,{error:"Local stop set; signer stop not confirmed. Stop signer service manually."})}}return send(res,200,{ok:true})}if(req.method==="POST"&&u.pathname==="/api/tick"){void trader.cycle();return send(res,202,{started:true})}if(req.method==="POST"&&u.pathname==="/api/backtest"){const observations=store.observations(),orders=store.orders(),closed=orders.filter(o=>o.mode==="paper"&&o.side==="sell"&&o.state==="confirmed"),profits=closed.map(o=>o.result.pnlUSDC),result={id:"evaluation:"+Date.now(),at:Date.now(),status:"INSUFFICIENT_EVIDENCE",observations:observations.length,signals:store.entities("signal",100000).length,closedPaperTrades:closed.length,realizedPaperPnl:profits.reduce((a,b)=>a+b,0),datasetHash:hash({observations,orders}),operatorApproved:false,routeSimulationValidated:false,successfulReconciliationTests:false,limitations:["Forward collected router quotes only; no claim of complete historical market coverage","Run root walk-forward replay on recorded signals and quote archive","Live approval requires independently reviewed out-of-sample evidence"]};store.entity("experiment",result.id,result);await store.flush();return send(res,200,result)}send(res,404,{error:"Not found"})}catch(e){log("request_failed",{reason:safeMessage(e)});send(res,400,{error:safeMessage(e)})}})}
export async function startRuntime(){const c=config();if(c.apiToken.length<32)throw Error("ADMIN_TOKEN must be at least 32 characters");const persistence=c.persistence==="supabase"?new SupabasePersistence(c.supabaseURL,c.supabaseKey):null;const store=new Store(c.persistence==="supabase"?":memory:":c.db,persistence);if(persistence){await persistence.hydrate(store);log("supabase_hydrated",{})}const trader=new Trader(c,store),server=createApp(c,store,trader);server.requestTimeout=20000;server.headersTimeout=10000;server.listen(c.port,"0.0.0.0",()=>log("trader_started",{port:c.port,mode:c.mode,persistence:c.persistence}));let timer;async function loop(){if(trader.stop)return;await trader.cycle();await store.flush();timer=setTimeout(loop,c.pollMs)}void loop();let closing=false;async function shutdown(){if(closing)return;closing=true;trader.stop=true;clearTimeout(timer);server.close();const poll=setInterval(async()=>{if(!trader.busy){clearInterval(poll);try{await store.flush()}finally{store.close();process.exit(0)}}},100);setTimeout(()=>process.exit(1),30000).unref()}process.on("SIGTERM",shutdown);process.on("SIGINT",shutdown)}
if(process.argv[1]===fileURLToPath(import.meta.url))startRuntime().catch(e=>{console.error(e);process.exitCode=1});
