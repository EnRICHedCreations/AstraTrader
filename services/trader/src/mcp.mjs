import { createMcpHandler, McpServer } from '@modelcontextprotocol/server';
import { toNodeHandler } from '@modelcontextprotocol/node';
import * as z from 'zod/v4';
const actionFor={inspect_asset:'inspect_asset',approve_asset:'approve_asset',revoke_asset:'revoke_asset',revoke_all_assets:'revoke_all_assets',pause_signals:'pause',resume_signals:'resume',kill_trading:'kill',execute_signal:'execute_signal',close_position:'close_position',set_strategy:'set_strategy',reset_strategy:'reset_strategy'};
async function local(c,path,method='GET',payload){const r=await fetch(`http://127.0.0.1:${c.port}${path}`,{method,headers:{authorization:'Bearer '+c.apiToken,'content-type':'application/json'},...(payload?{body:JSON.stringify(payload)}:{})});const j=await r.json();if(!r.ok)throw Error(j.error??`AstraTrader HTTP ${r.status}`);return j}
async function callTool(name,args,c){if(name==='get_status')return local(c,'/api/agent/status');if(name==='get_opportunities')return local(c,'/api/agent/opportunities');if(name==='get_orders')return local(c,'/api/agent/orders');if(name==='get_strategy')return local(c,'/api/strategy');const action=actionFor[name];if(!action)throw Error('Unknown MCP tool');return local(c,'/api/agent/action','POST',{action,...(args??{})})}
function authorized(ctx,c){return ctx?.http?.req?.headers?.get('authorization')==='Bearer '+c.apiToken}
function result(data){return{content:[{type:'text',text:JSON.stringify(data)}],structuredContent:data}}
function register(server,c,name,config,schema){server.registerTool(name,{...config,inputSchema:schema},async(args,ctx)=>{if(!authorized(ctx,c))return{content:[{type:'text',text:'Unauthorized. Connect Astra Trader with the configured Bearer API key.'}],isError:true};try{return result(await callTool(name,args,c))}catch(e){return{content:[{type:'text',text:String(e?.message??e)}],isError:true}}})}
function buildServer(c){const s=new McpServer({name:'Astra Trader',version:'2.0.0'},{capabilities:{tools:{listChanged:false}}}),empty=z.object({}),strategyPatch=z.record(z.string(),z.union([z.number(),z.boolean()]));register(s,c,'get_status',{title:'Get AstraTrader status',description:'Read current mode, health, P&L, positions, live readiness and active strategy.',annotations:{readOnlyHint:true,destructiveHint:false,openWorldHint:false}},empty);register(s,c,'get_opportunities',{title:'Get trading opportunities',description:'Read current signals, eligibility and evidence.',annotations:{readOnlyHint:true,destructiveHint:false,openWorldHint:false}},empty);register(s,c,'get_orders',{title:'Get order journal',description:'Read current and recent paper or live orders.',annotations:{readOnlyHint:true,destructiveHint:false,openWorldHint:false}},empty);register(s,c,'get_strategy',{title:'Get strategy configuration',description:'Read every runtime-configurable AstraTrader strategy parameter and its validation schema.',annotations:{readOnlyHint:true,destructiveHint:false,openWorldHint:false}},empty);register(s,c,'set_strategy',{title:'Change trading strategy',description:'Change one or more AstraTrader strategy parameters at runtime. Use this to control wallet qualification, correlation requirements, token filters, freshness, sizing, exposure, stops, targets, hold time, drawdown and signal scoring. Changes persist.',annotations:{readOnlyHint:false,destructiveHint:false,openWorldHint:false}},z.object({strategy:strategyPatch,replace:z.boolean().optional()}));register(s,c,'reset_strategy',{title:'Reset trading strategy',description:'Reset all runtime strategy parameters to application defaults.',annotations:{readOnlyHint:false,destructiveHint:true,openWorldHint:false}},empty);register(s,c,'inspect_asset',{title:'Inspect asset',description:'Inspect a Solana mint under the active strategy.',annotations:{readOnlyHint:true,destructiveHint:false,openWorldHint:true}},z.object({mint:z.string().min(32).max(44)}));register(s,c,'approve_asset',{title:'Approve asset policy entry',description:'Add or refresh a mint in asset policy. Strategy controls whether asset review is required for trading.',annotations:{readOnlyHint:false,destructiveHint:false,openWorldHint:true}},z.object({mint:z.string().min(32).max(44),evidence:z.string().min(20),reviewedBy:z.string().optional(),days:z.number().int().min(1).max(365).optional()}));register(s,c,'revoke_asset',{title:'Revoke asset approval',description:'Remove a mint from asset policy.',annotations:{readOnlyHint:false,destructiveHint:true,openWorldHint:false}},z.object({mint:z.string().min(32).max(44)}));register(s,c,'revoke_all_assets',{title:'Revoke all assets',description:'Remove every approved asset.',annotations:{readOnlyHint:false,destructiveHint:true,openWorldHint:false}},empty);register(s,c,'pause_signals',{title:'Pause signals',description:'Pause autonomous signal evaluation.',annotations:{readOnlyHint:false,destructiveHint:false,openWorldHint:false}},empty);register(s,c,'resume_signals',{title:'Resume signals',description:'Resume signal evaluation.',annotations:{readOnlyHint:false,destructiveHint:false,openWorldHint:false}},empty);register(s,c,'kill_trading',{title:'Kill trading',description:'Stop trading immediately; live mode also requests signer shutdown.',annotations:{readOnlyHint:false,destructiveHint:true,openWorldHint:false}},empty);register(s,c,'execute_signal',{title:'Execute accepted signal',description:'Execute an existing eligible signal.',annotations:{readOnlyHint:false,destructiveHint:true,openWorldHint:true}},z.object({signalId:z.string().min(1)}));register(s,c,'close_position',{title:'Close paper position',description:'Close an open paper position.',annotations:{readOnlyHint:false,destructiveHint:true,openWorldHint:true}},z.object({mint:z.string().min(32).max(44)}));return s}
let nodeHandler=null,boundConfig=null;function getHandler(c){if(nodeHandler&&boundConfig===c)return nodeHandler;boundConfig=c;nodeHandler=toNodeHandler(createMcpHandler(()=>buildServer(c),{responseMode:'json'}));return nodeHandler}
export async function handleMcp(req,res,c){const url=new URL(req.url,'http://localhost');if(url.pathname!=='/mcp')return false;await getHandler(c)(req,res);return true}
