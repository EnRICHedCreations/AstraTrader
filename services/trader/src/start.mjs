import {createServer} from 'node:http';
import {startRuntime} from './server.mjs';
import {integer} from './config.mjs';
export function missingSetup(e){const supabase=(e.PERSISTENCE_BACKEND==='supabase'||!!e.SUPABASE_URL);return [['ADMIN_TOKEN',e.ADMIN_TOKEN?.length>=32],['PERSISTENCE_BACKEND',supabase||!!e.DATABASE_PATH],['SUPABASE_URL',!supabase||!!e.SUPABASE_URL],['SUPABASE_SERVICE_ROLE_KEY',!supabase||e.SUPABASE_SERVICE_ROLE_KEY?.length>=20],['PERSISTENCE_CONFIRMED',e.PERSISTENCE_CONFIRMED==='true']].filter(x=>!x[1]).map(x=>x[0])}
const missing=missingSetup(process.env);
if(!missing.length)startRuntime();
else{const port=integer(process.env.PORT??8080,'PORT',1,65535);const server=createServer((req,res)=>{if(req.url==='/healthz'){res.writeHead(200,{'content-type':'application/json'});return res.end(JSON.stringify({alive:true,configured:false,trading:false,missing}))}if(req.url!=='/'){res.writeHead(503,{'content-type':'application/json'});return res.end(JSON.stringify({error:'Setup required',missing}))}res.writeHead(200,{'content-type':'text/html; charset=utf-8','content-security-policy':"default-src 'none'; style-src 'unsafe-inline'; base-uri 'none'; frame-ancestors 'none'",'cache-control':'no-store','x-content-type-options':'nosniff'});res.end(`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>AstraTrader · Setup</title><style>body{font:16px/1.6 system-ui;background:#0b1014;color:#e1e9ee;max-width:800px;margin:60px auto;padding:24px}h1,b,a{color:#a4eead}code{background:#172129;padding:4px 8px}li{margin:12px 0}section{padding:24px;border:1px solid #293740;border-radius:8px}</style><h1>AstraTrader is deployed.</h1><p>Trading and data collection are locked until secure setup is complete.</p><section><h2>Required configuration</h2><ul>${missing.map(m=>`<li><code>${m}</code></li>`).join('')}</ul><p>For hosted paper mode, set <code>PERSISTENCE_BACKEND=supabase</code>, your Supabase project URL and service-role key, then set <code>PERSISTENCE_CONFIRMED=true</code> after the schema is installed. Keep <code>TRADING_MODE=paper</code>.</p><a href="https://github.com/EnRICHedCreations/AstraTrader/blob/main/docs/USER_GUIDE.md">Open the complete setup guide →</a></section><p>No wallet keys are requested here. Live signing is disabled.</p></html>`)});server.listen(port,'0.0.0.0',()=>console.log(JSON.stringify({event:'setup_required',missing})));process.on('SIGTERM',()=>server.close(()=>process.exit(0)))}
