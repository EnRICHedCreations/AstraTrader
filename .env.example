# Optional for small public-RPC experiments; strongly recommended for sustained ingestion.
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# Required only for the standalone collector. Generate a random 32-byte value.
COLLECTOR_TOKEN=
# Standalone runner only (not browser variables)
ACTOR_URL=
SITES_ACCESS_COOKIE=
